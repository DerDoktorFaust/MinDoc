title: Title
email: your-email@example.com
description: by Authors

baseurl: "/MinDoc" # the subpath of your site. Change it if the name of your repository is other than "mindoc"

twitter_username: jekyllrb
github_username:  jekyll

# Custom directory structure
layouts_dir: configuration_files/_layouts
includes_dir: configuration_files/_includes

sass:
  load_paths:
    - configuration_files/_sass


# Build settings

plugins:
  - jekyll-feed

collections:
  mindoc_media:
    output: true
    layout: 'image_description'
